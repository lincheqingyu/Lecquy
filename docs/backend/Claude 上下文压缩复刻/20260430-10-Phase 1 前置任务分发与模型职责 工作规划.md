# Phase 1 前置任务分发与模型职责

更新日期：2026-04-30

> 本文档把 Phase 1 编码前的 5 项前置工作（文档 7 收敛后识别出的 gap）按 Haiku / Sonnet / Opus 三个模型分发，明确每项的输入、交付物、行为约束、验收标准与依赖关系。
>
> 上游依赖：文档 6（事实对比）、文档 7（收敛后的设计）、文档 8（审查清单）、文档 9（实施方案，待修订）。

---

## 1. 模型分工原则

| 模型 | 适合的任务类型 | 不适合的任务类型 |
| --- | --- | --- |
| Haiku | 机械执行：code reading 与事实核实、按既定模板做增量编辑、逐行对照清单 | 跨文档对齐、设计判断、长篇结构化撰写 |
| Sonnet | 工程执行：长文档修订、代码实施、单测与集成测试、轻量工程设计 | 跨维度的策略权衡、首次设计推敲 |
| Opus | 设计判断：开放问题收敛、跨场景策略、上线节奏、最终一致性审查 | 大批量代码修改、机械文档编辑 |

按这个分层，Phase 1 编码本身（按文档 9 修订版执行 8 步）交给 Sonnet；本轮的设计推敲（文档 7 收敛、本文档）由 Opus 完成；Haiku 承担核查与清单更新。

---

## 2. 任务总览

| 任务 | 名称 | 模型 | 依赖 | 交付物形态 |
| --- | --- | --- | --- | --- |
| T1 | 核实五项工程事实 | Haiku | 无 | 一份事实备忘文档（新建） |
| T2 | 文档 9 按文档 7 收敛结论修订 | Sonnet | T1 | 修订后的文档 9（覆盖原文件） |
| T3 | 配置层字段落点设计 | Sonnet | T1 | 并入 T2 的修订内容 |
| T4 | 文档 8 审查清单增补 | Haiku | 无 | 修订后的文档 8（覆盖原文件） |
| T5 | 灰度与回滚策略 | Opus | 无 | 一份策略文档（新建） |
| T6 | Phase 1 编码实施 | Sonnet | T1-T5 全部完成 | compact.ts / template / runtime-service 改动 + 测试 |

T1、T4、T5 可并行启动；T2 与 T3 由同一个 Sonnet session 一起完成；T6 是 1-5 全部完成后的下一阶段动作，本文档只提一次，不展开。

---

## 3. T1：核实五项工程事实（Haiku）

### 3.1 任务说明

文档 7 第 5 节定义的接口需要落到现有代码上才生效。Haiku 负责把以下五个事实读出并落字。这是纯事实核查任务，不需要做任何设计判断。

### 3.2 输入

- 文档 7 第 5.1、5.3、5.4、5.5 节
- 代码读取目标：
  - `backend/src/session-v2/session-service.ts` 中 `createSimpleTitle()` 的 `completeSimple` 调用
  - `backend/node_modules/@mariozechner/pi-ai/dist/**` 中 `completeSimple` 的类型定义与导出
  - `backend/src/runtime/session-runtime-service.ts` 中 `bound.projection.model` 与 `apiKey` 的真实来源
  - `backend/src/config/**` 的 `Env` 类型定义
  - `@lecquy/shared` 中 `extractSessionText` 的实现
  - `backend/src/memory/compact.test.ts` 现状

### 3.3 交付产物

新建文件：`docs/backend/Claude 上下文压缩复刻/20260430-11-Phase 1 工程事实备忘.md`

文档结构（每节回答一个事实问题，给出代码引用 + 行号 + 一句结论）：

1. `completeSimple` 的真实签名（参数名、是否支持 `signal`、`maxTokens` 字段名、错误类型）
2. `bound.projection.model` 的实际类型（是否为 `Model<'openai-completions'>` 或需要 service 层转换）
3. compaction 应该从哪里取 apiKey（`cfg.LLM_API_KEY` 还是 `cfg.ANTHROPIC_API_KEY`）
4. `extractSessionText` 在 `tool_use` / `tool_result` parts 上的行为（是返回参数 JSON、纯文本，还是空字符串）
5. `compact.test.ts` 当前的 mock 方式（模块级 mock、构造函数注入、还是其他）

### 3.4 行为约束

- 只读不改：本任务**不**修改任何代码或现有文档
- 每条结论必须附 `file_path:line` 形式的代码引用
- 遇到模糊点不要猜，直接在该条下面写"无法确定，建议下一步：……"
- 不做设计推断，不写"建议怎么改"，只写事实
- 文档不超过 400 行

### 3.5 验收标准

- 五项事实每项都有明确结论或"无法确定"标注
- 引用的代码位置准确（执行 Read 后能立即定位）
- 不包含设计判断或任务建议

---

## 4. T2：文档 9 按文档 7 收敛结论修订（Sonnet）

### 4.1 任务说明

文档 9 是 4 月 29 日写的 Phase 1 实施方案，文档 7 在 4 月 30 日做了两轮收敛后产生了与文档 9 冲突的结论。把文档 9 重写一遍，使其完全对齐文档 7 第 4 节（4 个收敛结论）和第 5 节（8 项前置约束）。

这不是新增章节，是覆盖原有的第 4-9 节。

### 4.2 输入

- 文档 7（最新版，作为唯一的设计依据）
- 文档 9（原版，作为结构参考）
- T1 的事实备忘（决定接口签名、apiKey 字段、Model 类型等具体细节）

### 4.3 交付产物

修订后的 `docs/backend/Claude 上下文压缩复刻/20260429-9-压缩 Agent 与摘要生成实施方案 技术规范.md`（覆盖原文件，更新文档头日期为 2026-04-30 并保留首版日期）。

具体要修订的章节：

| 章节 | 当前内容 | 修订要求 |
| --- | --- | --- |
| 第 4.1 节接口设计 | `buildCompactSummary` 入参含 `llmClient` | 改为：从 `model` + `apiKey` 直接调 `completeSimple`，不再暴露 `llmClient` 参数 |
| 第 4.2 节 message 组织 | "序列化为单条 user 消息文本块" | 落实文档 7 第 4.1 节的"工具调用嵌入助手段落、`↳ tool(args) → output`"格式，给出完整序列化示例 |
| 第 4.3 节 SUMMARY_TEMPLATE | 全部塞在 user 消息 | 按文档 7 第 5.2 节拆为 system prompt（指令）+ user prompt（数据） |
| 第 4.4 节 prompt 构建 | 单一函数返回 string | 返回 `{ system: string; user: string }` |
| 第 5 节降级 | `console.warn` + 旧 details 字段 | 改为 `logger.warn`，details 增加 `compaction_method` / `llm_error`（按文档 7 第 4.2 节） |
| 第 6 节 `applyCompactionIfNeeded` 签名 | 闭合 `CompactionOptions` | 按文档 7 第 5.5 节改为开放结构，明确 Phase 2 预留字段 |
| 第 7 节 model 选择 | 单段建议 | 按文档 7 第 5.3 节增加降级触发条件 |
| 新增"超时与上限" | （不存在） | 按文档 7 第 5.4 节新增一节，说明 `maxTokens=4096`、`compactionTimeoutMs` 配置项、`toolOutputMaxChars=2000` |
| 新增"writeMemorySummary 同源" | （不存在） | 按文档 7 第 5.6 节新增一节 |
| 第 8 节测试策略 | 三类测试 | 按文档 7 第 5.7 节扩充：超时单测、previousSummary 完整传入单测、tool 配对断裂场景 |
| 第 9 节实施顺序 | 8 步 | 与文档 7 第 6 节的 8 步对齐（Step 标号一致） |

### 4.4 行为约束

- **不**重新做设计判断。文档 7 已经收敛的结论直接抄过来，不要再写"建议……"或"考虑……"
- **不**改文档 7 / 文档 8。本任务只动文档 9
- 代码示例中所有具体细节（接口签名、字段名）必须与 T1 的事实备忘一致
- 保持文档 9 原有的章节顺序与小标题结构，仅替换内容
- 中文文档规范（同项目语言规范）：注释、正文中文，类型 / 字段 / 文件名英文
- 修订后必须自检：每条文档 7 第 4 节与第 5 节的结论都能在新版文档 9 中找到落地位置

### 4.5 验收标准

- 文档 9 与文档 7 第 4 节、第 5 节零冲突
- 文档 9 中每个接口签名 / 字段名 / 配置项与 T1 的事实备忘一致
- 文档 9 第 9 节的实施顺序与文档 7 第 6 节的 Step 标号完全对应
- 编码 agent 拿到修订后的文档 9 + T1 备忘后，可以直接开始改 compact.ts 而不需要回看文档 7

---

## 5. T3：配置层字段落点设计（Sonnet，并入 T2）

### 5.1 任务说明

文档 7 第 5.4 节要求 `compactionTimeoutMs` 做成可配置项，但没指定字段位置。本任务是定下三个值的落点：

- `compactionTimeoutMs`：起步 60_000，env 暴露给运维
- `compactionMaxOutputTokens`：起步 4_096，service 默认值
- `compactionToolOutputMaxChars`：起步 2_000，service 默认值

### 5.2 输入

- T1 备忘中关于 `Env` 类型的部分
- 文档 7 第 5.4 节

### 5.3 交付产物

落地内容并入 T2 修订后的文档 9，单独成节"配置项与默认值"，包含：

- 三个字段的最终命名（与现有 `Env` 类型风格一致）
- 在 `cfg` 中的层级（顶层还是嵌套在 `cfg.compaction.*`）
- 默认值与可改动范围
- env 变量名（仅 `compactionTimeoutMs` 暴露 env）

### 5.4 行为约束

- 字段命名必须遵循现有 `Env` 类型的命名风格（驼峰 / 下划线，看现状）
- **不**实际改 `Env` 类型代码，只在文档里定义。代码改动留给 T6
- 三个字段如果命名风格冲突现有 cfg，以现状一致为准

### 5.5 验收标准

- 文档 9 修订版包含独立的"配置项与默认值"小节
- 字段命名与现有 cfg 风格一致
- env 暴露策略说明清楚

---

## 6. T4：文档 8 审查清单增补（Haiku）

### 6.1 任务说明

Phase 1 上线后多了几条新约束。把它们加进文档 8（审查清单），保持清单与实际约束同步。

### 6.2 输入

- 文档 8 现状
- 文档 7 第 4 节、第 5 节、第 7 节

### 6.3 交付产物

修订后的 `docs/backend/Claude 上下文压缩复刻/20260429-8-上下文压缩关键点审查清单 技术规范.md`（覆盖原文件，更新文档头日期为 2026-04-30 并保留首版日期）。

需要增补的条目：

1. **第 3 节"摘要生成相关"**新增：
   - compaction entry 的 `compaction_method` 字段是否正确写入（`'llm' | 'template'`）
   - LLM 失败路径是否走 `logger.warn` 而非 `console.warn`
   - LLM 失败时 `details.llm_error` 是否被记录

2. **第 4 节"裁剪边界相关"**新增：
   - `formatHistoryForCompaction` 序列化是否正确处理了 tool_use / tool_result 切口断裂场景

3. **第 5 节"上下文组装顺序相关"**新增：
   - `writeMemorySummary` 写入文件的内容是否与 compaction entry summary 字段同源

4. **第 10 节"快速参考"**新增三个常量：
   - `compactionTimeoutMs`（默认 60_000）
   - `compactionMaxOutputTokens`（默认 4_096）
   - `compactionToolOutputMaxChars`（默认 2_000）

5. **第 9 节"已知坑"**新增一条：
   - `bound.projection.model` 在非 Anthropic provider 下 `completeSimple` 不可用，必须走模板降级（具体表现详见 T1 备忘）

### 6.4 行为约束

- 严格按文档 8 既有的章节风格（checkbox 列表 + 简短说明 + 已知风险标注）
- **不**重写已有条目，仅追加
- **不**修改文档 7 / 文档 9
- 每条新增条目必须能在文档 7 中找到对应的设计依据，并在条目末尾用 `（依据：文档 7 X.Y）` 标注
- 文档总长度增量控制在 60 行以内

### 6.5 验收标准

- 五处增补全部到位
- 清单条目与文档 7 收敛结论可逐条对应
- 文档 8 的章节顺序、checkbox 风格、命名规范与原版一致

---

## 7. T5：灰度与回滚策略（Opus）

### 7.1 任务说明

Phase 1 是触达每一次 run 末尾的改动，回滚成本不低。设计一份灰度上线与回滚策略。三个未决问题：

- 是否加 feature flag 让旧模板路径与新 LLM 路径并存
- 出问题时回滚是改 flag 还是 revert PR
- 摘要质量回退如何发现，要不要在 compaction entry 里多存 history 指纹

### 7.2 输入

- 文档 7 全文
- 文档 6 关于"压缩任务的可观测性"的对比（OpenCode 把压缩做成消息流可见，Lecquy 是内部 entry）
- 现有 cfg / env 体系（仅作背景，不细看）

### 7.3 交付产物

新建文件：`docs/backend/Claude 上下文压缩复刻/20260430-12-Phase 1 灰度与回滚策略 技术规范.md`

文档结构：

1. 上线节奏（按多少天观察、看哪些指标决定推进）
2. feature flag 设计（字段位置、默认值、回滚动作）
3. 可观测性指标（降级率、摘要长度分布、压缩耗时分布、采样审计机制）
4. 回滚预案分级（flag 关闭 / 配置回退 / revert PR / 数据修复）
5. 摘要质量审计抽样（上线初期人工抽查策略）

### 7.4 行为约束

- 给出**判断**而不是"列举可能选项"。每个开放问题都要有明确建议方向 + 决策理由
- 与文档 7 / 文档 8 / 文档 9 的现有决策保持一致，**不**触发任何重新讨论
- 篇幅控制在 200 行以内，按文档 7 / 8 / 9 的密度走
- 中文文档规范同上

### 7.5 验收标准

- 三个未决问题每个都有明确收敛结论
- 提出的可观测指标在 Lecquy 现有日志体系下可实现（不引入新基础设施）
- 回滚预案与 feature flag 设计能在 1 个工作日内回滚（不需要数据迁移）

---

## 8. T6：Phase 1 编码实施（Sonnet，依赖 T1-T5）

T1-T5 完成后，把修订版的文档 9 + T1 事实备忘 + T5 灰度策略一起交给 Sonnet 执行编码。Sonnet 按文档 9 第 9 节的 8 步顺序完成 `compact.ts`、`compact-summary.template.ts`、`session-runtime-service.ts` 的改动，并写单元测试 / 集成测试。

本文档不展开 T6 细节，编码阶段以修订后的文档 9 为唯一执行依据。

---

## 9. 依赖与并行

```
T1 (Haiku, 事实核实) ──┬──> T2/T3 (Sonnet, 文档 9 修订) ──┐
                       │                                   │
T4 (Haiku, 文档 8)  ────────────────────────────────────────┤
                                                           │
T5 (Opus, 灰度策略) ────────────────────────────────────────┤
                                                           ▼
                                                   T6 (Sonnet, 编码)
```

可并行：T1、T4、T5 同时启动。
必须串行：T2/T3 等 T1，T6 等 T1-T5 全部完成。

---

## 10. 模型行为通用准则

无论交给哪个模型，以下三条共用约束：

1. **遵循 `.claude/rules/language.md`**：正文中文、代码 / 类型 / 文件名英文、首次出现技术术语可附英文原文
2. **不擅自做超出本任务范围的改动**：只动本文档列出的目标文件，其他文档与代码不动
3. **遇到与文档 7 收敛结论冲突的边界**：在交付物末尾单独列"待复核"小节，不擅自重新决策

后续 T2-T6 的执行 agent 收到的 prompt 必须显式引用本文档对应任务节，并把上述三条作为硬约束传入。
